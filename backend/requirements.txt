# Virtual Try-On Backend requirements
# Python 3.10 (venv)

# Ensure pip can resolve CUDA-enabled PyTorch wheels.
--extra-index-url https://download.pytorch.org/whl/cu121

# Core ML stack
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121
numpy==1.26.4

# API
fastapi==0.135.2
uvicorn[standard]==0.42.0
python-multipart==0.0.22

# Imaging and preprocessing
pillow==12.1.1
opencv-python==4.9.0.80
scipy==1.15.3
einops==0.8.2
tqdm==4.67.3

# Inference/runtime
onnxruntime==1.23.2
requests==2.32.5
python-dotenv==1.2.2

# FASHN VTON model/runtime deps used by local source package
fashn-human-parser==0.1.1
safetensors==0.7.0
huggingface-hub==0.36.2     # 1.x requires torch>=2.4, incompatible
transformers==4.44.2         # 5.x requires torch>=2.4, incompatible
accelerate>=0.30.0

# Super-resolution stack
basicsr==1.4.2
realesrgan==0.3.0
facexlib==0.3.0
gfpgan==1.3.8
codeformer-pytorch>=0.1.0     # Problem 4A: better face restoration (preferred over GFPGAN)

# Mask refinement (Problem 1 — optional, degrades gracefully)
segment-anything-2>=0.1.0     # SAM2 for pixel-perfect clothing masks

# Color harmonization (Problem 2)
scikit-image>=0.22.0          # match_histograms for garment color correction

# Background removal — replaces busy backgrounds with plain white for cleaner results
rembg>=2.0.50

# IDM-VTON (primary pipeline — local GPU inference)
diffusers==0.25.1             # IDM-VTON requires 0.25.x (see cached_download patch note)
controlnet-aux>=0.0.7         # OpenPose preprocessing for IDM-VTON

# Explicit pins to avoid llvmlite source-build failures on Python 3.10
numba==0.59.1
llvmlite==0.42.0
